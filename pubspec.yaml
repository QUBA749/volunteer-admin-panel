name: admin_panel
description: Volunteer Management Admin Panel
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  shared:
    path: ./shared

  http: ^1.2.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  flutter_secure_storage: ^9.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
